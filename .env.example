# URL de la base de datos SQLite.
# Prisma crea el archivo automáticamente si no existe.
# Para tests, se puede apuntar a una base en memoria o a un archivo temporal.
DATABASE_URL="file:./certichain.db"
DATABASE_URL_PG=postgresql://postgres:admin@localhost:5432/certichain